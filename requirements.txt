# Tested with Python 3.12.
# Core scientific stack pinned to the environment used to run the full cohort analysis.
numpy==2.3.3
pandas==2.3.3
scipy==1.16.2
matplotlib==3.10.7
networkx==3.5

# Notebook stack so the reporting notebook can be opened and executed from a clean environment.
jupyterlab>=4.2,<5
ipykernel>=6.29,<7
